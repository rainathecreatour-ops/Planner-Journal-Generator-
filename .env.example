DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DB?schema=public"

# Access-code cookie secret-ish (used for your own ops; keep long/random)
AUTH_SECRET="replace-with-long-random-string"

# Protect admin endpoints (creating codes)
ADMIN_SECRET="replace-with-strong-password"

# Optional: auto-create a demo code on first redeem call
SEED_DEMO_CODE="true"
